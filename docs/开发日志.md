# 开发日志

只记录真实发生的工作。当前对话同时保留调研、决策和 AI 辅助过程；没有伪造历史提交。

当前状态与后续待办先看根目录 [项目档案](../项目档案.md)。下方各日期记录保留当时状态，不将旧的“未领取、暂停”等描述视为当前结论。

## 2026-09-09 · 参赛准备

### 用户与范围

- 用户确认已报名，单人参赛，知识方向；不知道是否已领取 API 凭证。
- 创建/领取/提交等平台动作尚未执行。
- 主代理负责核对规则、接口抽查、方案整合与验证。
- GPT-5.6 Terra（high）负责独立 API 文档评估与数据模块骨架。
- GPT-5.6 Sol（xhigh）负责独立检查产品范围与演示逻辑。

### 来源和操作

- 通过浏览器读取官方开发者手册、流程、FAQ。
- 获取并解压官方 skill 到项目 `vendor/zhihu-skill-0.5.3/zhihu`，未放入全局自动发现目录。
- 压缩包 SHA-256：`F7B1DE244C875749FEEC7FAE5B134E2DE5F26332198E6C73861140B2D72C4DD7`。这是本地完整性记录，没有额外供应商签名认证含义。
- 官方 `run.ps1 status` 返回 `installed=false`；没有安装 CLI。赛事内容 API 不依赖 CLI。
- 使用官方 HTTPS GET 获取一份知识列表、两篇选定详情；未扫描全库正文，未爬取知乎网页。

### 实测数据

| 请求 | 保存位置 | 结果 |
|---|---|---|
| `/knowledge/list` | `sources/knowledge-list-2026-09-09.json` | JSON 数组，10项 |
| `/knowledge/1523701957479239680` | `sources/knowledge-detail-1523701957479239680.json` | 作者刀熊说说，content 3000字符，末尾截断 |
| `/knowledge/1393937473601368064` | `sources/knowledge-detail-1393937473601368064.json` | 作者王明伟，content 3000字符，末尾截断 |

固定前缀：`https://api.zhihu.com/km-indep-home/hackathon/v2`。请求未带鉴权；curl 退出码0、成功解析 JSON。两次详情只用于验证返回范围，不证明所有内容都截断于同一长度，也不证明部署平台可连通。

### 关键决策

- 正式赛道核对为「知识炼金场：学习工具与知识生产」。
- 从宽泛课程辅导收窄为“读懂建议—理解核对—小行动”，因为实际供给没有专业课题库。
- 不承诺完整原文；不把作者观点等同于客观正确。
- 异常标签不作为可靠分类依据；具体样本中的职场/学习内容带有故事类标签。
- API 内容读取与生成模型是两条不同依赖。前者已验证，后者尚未配置。
- 初步内部目标为9/14晚完成最终提交，团队安排待确认。

### 验证记录

用户随后要求先澄清48小时开发、算力资源和报名/建队关系，已暂停代码准备。

已写入 `package.json`、`src/zhihu-knowledge.mjs`、`scripts/probe-knowledge.mjs`、`tests/zhihu-knowledge.test.mjs`；**均未进行语法检查、测试执行或脚本运行**，不声称模块可用。前文真实 API 结果来自独立 curl 请求，不能代替代码验证。

正式作品尚未开始验收，未部署、未创建队伍、未领取补贴、未提交。活动页在当前浏览器未登录，无法据此核验用户队伍状态。

## 2026-09-09 · 技能与团队仓库准备

- 用户正在拉队友；原“单人参赛”记录不再代表最终团队规模。
- 用户要求等待额度后再正式开发，同时授权下载有用技能并创建 GitHub 仓库。
- 通过内置 Skill Installer，从固定 Git 提交安装 Anthropic `frontend-design` 和 OpenAI `security-best-practices`。安装输出成功，已读取安装目录；本次未应用它们开发产品。
- 准备按私有仓库 `lucchinopolare-cmd/zhihu-hackathon-2026` 同步调研、官方接口文档与原有未验证骨架。新增团队协作和技能来源说明。
- 原始内容样本、下载包、个人课表草案不进入版本控制。拟上传文件经过静态检查，未发现实际凭证；此检查不是应用安全验收。
- 未运行应用、测试或 API 探测，未改动模型配置、领取额度或邀请队友。

## 2026-09-09—09-11 · 协作与赛事模型接入

- 已创建 GitHub 私有仓库 `lucchinopolare-cmd/zhihu-hackathon-2026`，初始化提交 `2cd7cbf` 已推送。
- 按用户明确授权向 `A-yin` 发出 write 邀请，已核对待接受状态；此后是否接受尚未刷新。
- 用户截图显示 500 USD 已领取并分配给 Miku；当前余额未查询。
- 2026-09-11：赛事模型列表请求成功；Astra + ultra 被平台以 HTTP 400 拒绝，用户明确选择 Astra + max。max 的 Responses 请求返回 HTTP 200、completed，并输出 OK。
- 已备份并增量配置 Codex 用户级 provider，以独立环境变量保存 Key，未读取或覆盖 `auth.json`。Codex 命令行在空目录使用核心 provider 参数测试成功，退出码 0；未覆盖桌面插件与当前聊天计费路由的验证。
- API 连通验证不是项目代码验收；原有项目骨架仍未运行、未测试。

## 2026-09-11 · 建立赛事要求与项目主档案

- 用户明确提出准备干项目，先把题目要求等保存为文件；据此恢复项目准备。
- 重新通过浏览器核对手册、流程和 FAQ 的相关段落，补齐初审 40/25/25/10 与决赛 35/25/25/8/7 两套评分权重，并区分必交与选交材料。
- 新建根目录 `项目档案.md`，集中记录官方要求、当前事实、候选题目、接口边界、待办与来源；新建简短 `AGENTS.md` 要求后续开工先读档案。
- 更新 README、协作说明、候选方案与技能文档中的过期暂停/额度状态；保留旧日期日志，不将候选方案升级为已定题。
- 本轮只整理文档，不运行项目或模型 API，不改动凭证，不宣称作品完成。

## 2026-09-11 · 核对当前 Codex 模型与推理模式

- 用户要求深层核查当前水平。通过本任务 session 日志确认：`model=gpt-6-astra`、`reasoning_effort=ultra`、`provider=openai-next`；这是当前桌面任务的运行时记录，不只是配置文件内容。
- 官方 ChatGPT Learn 模型文档说明：Extra High 是推理强度选项；Ultra 是使用子代理并行处理可拆分复杂工作的模式，不能简单视为同一条“高低”刻度。来源：[Models | ChatGPT Learn](https://learn.chatgpt.com/docs/models?surface=app)。
- 纠正此前表述：不能把本次桌面任务与赛事 API/CLI 探针中的 Astra + max 混为一谈；后者仍是独立接入验证。

## 2026-09-11 · 恢复骨架验证

- 接续“知乎黑客松 2026”项目后，先审计现有 Node 24 骨架与候选方案；确认尚无网页、运行时模型、部署或正式提交。
- `npm.cmd test` 实测通过：8 项测试全部通过。
- 首次 `npm.cmd run probe` 暴露离线样本路径错误：脚本已定位项目根目录，却继续使用 `../sources`，导致读取 `D:\sources` 并报 `ENOENT`。
- 修复为项目根下的 `sources/...` 后，离线 probe 实测通过：列表 10 项；选定 `work_id` 为 `1523701957479239680`；作者“刀熊说说”；正文 3000 字符；末尾截断信号 `possible`，完整性仍未知。
- `git diff --check` 未发现空白错误。上述结果只证明本地骨架和本机样本链路，未证明公网部署或应用模型接入。
- 证据边界：核验的是客户端已应用设置；日志没有独立的 outbound Responses 请求体或 Ultra → max 映射事件，服务端最终模型/档位及余额扣减仍未核实。脱敏字段与源日志行号仅保存在本机 `.local/current-codex-runtime-check-2026-09-11.json`。该项设置核验未修改模型配置、未另发模型 API 探针、未运行应用测试。

## 2026-09-11 · 需求文档与 GitHub skill 检索

- 用户明确要求允许用户跳过复述直接使用 AI；已新增 [产品需求文档](产品需求文档.md)，定义直接 AI 与自行复述两条入口、来源标识、引用核对、行动建议和 Markdown 导出验收标准。
- 通过 GitHub API 检索 OpenAI 与 Anthropic 官方技能目录，确认 `frontend-design`、`webapp-testing` 与本项目当前阶段相关；未把部署 skill 提前安装为产品依赖。
- 当前 CODEX_HOME 的 skills 目录最初只有 `.system`；按固定提交 `34040c9c568585f6929bedeaad110ad08f079624` 实际安装 Anthropic `frontend-design` 与 `webapp-testing` 两项。已核对入口与许可文件，未运行其辅助脚本或网页测试。
- 同步项目档案、方向草案与 README：直接 AI 为推荐入口，用户可直接查看和导出；复述、修订与确认不再是生成行动卡的强制门槛。保留 AI/用户内容归属，不宣称用户已理解或行动已完成。

## 2026-09-11 · 本地首版开发与赛前验收

- 在分支 `codex/learning-workbench` 完成 Node 24 本地首版：知识列表/详情服务、Responses 模型客户端、直接 AI 与自行复述双入口、逐字引用校验及定位、可修改行动建议、Markdown 学习卡导出、请求取消和文章草稿隔离。
- 提交前最终自动化测试共 23 项通过；前后端 JavaScript 语法检查、离线 probe 与 `git diff --check` 通过。此结果只覆盖本地代码和本机样本。
- 使用可控 fake model 完成浏览器验收：直接讲解、自行复述反馈、错误恢复、引用定位、行动编辑和实际 Markdown 下载均通过；导出卡片保留 AI/用户归属及“待尝试”状态。
- 393px 手机视口无横向溢出，来源与学习区上下排列；浏览器控制台在补充本地 favicon 后无错误。桌面结果页也完成视觉复核。
- 经用户明确授权，应用后端使用本机受 Git 忽略的赛事密钥做真实生成。两次请求均到达赛事模型服务，但分别在 126.7 秒和 126.5 秒后返回上游 502；未得到真实生成结果，不能记为端到端通过。
- 用户随后说明赛事尚未开赛并要求停止重试。真实模型可用性、耗时和预算留到开赛后复验；未执行公网部署、真实用户试用或官方提交。
- 提交前独立审查发现复述反馈和适用条件可在无引用时通过校验；已改为逐条要求有效引用并补回归测试。匿名生成接口的累计额度保护列为公网部署前待办。

## 2026-09-11 · 深度讲解与继续追问

- 针对用户认为 AI 过于简单的反馈，把输出从摘要升级为：核心主张、作者公开写出的论证步骤、适用条件、AI 批判性提醒、AI 构造场景、建议追问和可修改行动。`logic` 表示对公开文本的论证重建，不输出模型隐藏思考过程。
- 新增 `POST /api/follow-up`。追问只读取当前文章片段；回答拆成 1–4 条陈述，每条必须绑定至少一个逐字存在于对应段落的引用。字符串匹配能证明引文存在，不能单独证明引用在语义上充分支持结论。
- 前端按文章保存追问记录，切换文章时用 AbortController、版本号和 work_id 校验取消并隔离旧请求，防止旧回答写入新文章；学习卡导出同时保留带引用的追问记录。
- 本地模拟运行状态在生成前和结果中明确标注，不代表赛事模型接通。本轮未调用赛事模型，也未访问赛事知识接口。
- 最终自动化测试 27 项通过，前后端语法检查与 `git diff --check` 通过。桌面浏览器实测深度生成、推荐追问、回答和引用定位均成功，控制台无错误；393px 无头 Chrome 复测 `scrollWidth=393`、单列布局、追问 1 条、控制台无错误。
- 独立审查由 GPT-5.6 Terra（high）完成，指出追问回答最初没有逐条关联引用；该结构缺口已修复并增加错误引用、缺少引用、未知引用 ID 和混文章导出测试。

## 2026-09-11 · 赛事来源为空时的赛前演示

- 用户截图显示正式来源一直停在加载状态，并指出比赛尚未开始、来源内容可能为空。确认原实现的 `live` 直接依赖赛事列表，而 `sample` 依赖 Git 忽略的本机历史文件，无法作为可复现的团队演示入口。
- 新增 `npm.cmd run demo`：使用仓库内两篇项目原创学习材料与确定性预设回答，不需要密钥，也不访问赛事内容接口或赛事模型。演示模型仍经过与真实模型相同的结果 schema、逐字引用和 citation ID 校验。
- 页面顶部、来源元数据、来源脚注、生成结果和 Markdown 导出均标注“开发演示/非知乎正式内容”。正式 `live` 列表为空时，页面说明赛事内容可能尚未开放，并引导维护者启动演示模式，不用虚构内容掩盖正式来源为空。
- 新增 demo 与正式空列表测试。独立审查发现端口占用时会误报启动成功、第二篇材料会复用第一篇的追问话术、相反复述也会被标为“理解到位”；现已分别修复，并增加跨文章追问与反向复述回归测试，总计 32 项通过。
- 浏览器实测原创材料加载、深度讲解、推荐追问和带引用回答成功，控制台无错误。本轮未调用赛事接口或赛事模型。
## 2026-09-12 · 生成接口防护与前端竞态修复

- 针对部署阻断，生成类 POST 接口现在默认要求同源 `Origin`；可用 `ALLOWED_ORIGIN` 收紧到固定公网来源，`REQUIRE_ORIGIN=false` 仅保留本地兼容开关。
- 新增进程级生成防护：按来源窗口限频（默认每分钟 5 次）与累计请求上限（默认 20 次），触发时返回 429 及 `Retry-After`；这是请求次数预算闸门，公网部署前仍需结合实际模型计费做更精细的 token/金额预算。
- 修复学习卡归属：场景化 AI 行动不会因为“按我的场景调整”而重复导出为“用户编辑”；只有用户实际修改行动字段才会追加用户编辑段。
- 修复入口切换草稿丢失：挑战回答和个人场景先恢复为文章级草稿；后续已进一步按 `direct`/`reflect` 入口隔离，避免不同题目之间串用。
- 修复异步返回竞态：挑战核对会核对返回前的最新回答；场景化行动会核对最新场景与行动字段，旧请求不会覆盖用户的新编辑。
- 修复重复挑战反馈：每次渲染先清空旧结果，页面只保留最新一份 `.challenge-result`。
- `npm.cmd test` 通过 40 项；前后端 `node --check` 与 `git diff --check` 通过。更新后的离线演示服务在 `http://127.0.0.1:3010/` 启动并完成 health/learn 验证；未调用赛事模型。
## 2026-09-12 · Word 学习卡导出

- 根据用户反馈，将面向用户的 Markdown 下载改为标准 `.docx` 文件；文档在浏览器内生成，不上传用户复述、追问或行动修改。
- Word 学习卡增加标题、来源信息表、分节标题、引用块、行动表格与“待尝试”提示，同时继续使用原有 work_id、引用 ID 和跨文章校验。
- 自动化测试 34 项通过；实际生成文件已通过 ZIP/XML 完整性检查、`python-docx` 读取和 Microsoft Word 只读打开验证。

## 2026-09-12 · 两类证据标签、理解挑战与场景化行动

- 用户确认保留“原文理解”和“AI 延伸”两类标签，不显示“证据不足”；据此更新页面、学习卡和产品需求文档。
- 生成结果新增带引用的一分钟理解挑战；新增 `/api/challenge` 核对接口，返回关键点反馈，只有遗漏时给一个变式题，不评分、不宣称掌握。
- 新增 `/api/personalize-action`，用户输入自己的场景后只生成行动、完成标志和回顾安排；页面与 Word 卡片继续标记“AI 延伸”“待尝试”。
- 开发演示模型提供两篇原创材料的确定性挑战与场景化行动；通过 `npm.cmd test`（37 项）、前后端 `node --check`、`git diff --check`，并用 `npm.cmd run demo` 实测 health、knowledge、learn、challenge、personalize-action 接口。
- 本轮未调用真实知乎内容接口或赛事模型，未进行公网部署和真实用户验收；未复制第三方代码、提示词、界面文案或素材。

## 2026-09-12 · 场景化行动质量修正

- 用户通过截图指出“高数学学习”前后几乎没有变化。复核确认旧 demo 只是把场景前缀拼到通用行动上，不属于真正的场景化规划。
- 已将 demo 改为按具体场景分支生成不同任务：例如“下周高数复习、每晚 30 分钟、卡在极限题”会得到 3 道基础题、卡住步骤记录、补 1 个前置概念和下一次对照检查。
- 强化真实模型提示词：要求先提取学科/任务、阶段、可用时间、目标产出或卡点，并让 task、completion、review 三者都因场景而改变；不做模型微调训练，先用结构化提示和结果约束。
- 重启离线 demo 并实测 `/api/personalize-action` 返回上述具体结果；新增回归测试后总计 38 项通过。

## 2026-09-12 · 挑战状态隔离与生成额度修正

- 将一分钟挑战的题目对应回答与核对反馈改为按 `direct`、`reflect` 入口分别保存；切换入口或在同一入口重新生成时，不再把旧题目下的回答和反馈带到新结果中。导出 Word 学习卡也只读取当前入口状态。
- 生成接口增加可选 Bearer 授权（`REQUIRE_GENERATION_AUTH` / `GENERATION_AUTH_TOKEN`）；Origin 校验继续作为默认来源约束。请求额度改为预留后提交，模型未配置、上游失败、取消或解析失败会释放预留，不消耗累计成功请求额度。
- 为生成结果的 `challenge.question` 增加 500 字最大长度，并补齐 `.env.example` 的 Origin、授权和限流配置；项目档案与产品文档统一记录 F01–F15 共十五项验收标准。
- `npm.cmd test` 通过 43 项；四个源文件 `node --check` 和 `git diff --check` 通过。未调用赛前真实赛事模型；跨模式浏览器回归随后在离线 demo 上执行。

## 2026-09-12 · 生成保护审查修正

- 修正额度闸门语义：失败、取消或解析失败只释放累计成功额度的预留，仍保留本次每分钟尝试次数；旧窗口请求释放时不会删除新窗口状态。
- 网页授权改为支持服务端管理的 HttpOnly 会话 Cookie 或受信任网关注入的 `X-Generation-Gateway-Token`；`public/app.js` 明确使用同源凭证，未嵌入静态 Token。Bearer 仍仅用于 API 客户端。
- `.env.example` 改为留空 `REQUIRE_GENERATION_AUTH`，填写任一服务端凭证时默认启用授权，并补充会话 Cookie/网关配置说明；`/api/challenge` 调用方提交的问题也限制为 500 字。
- 新增失败限频、跨窗口并发、会话/网关授权和挑战问题长度回归测试；`npm.cmd test` 通过 48 项，语法检查与 `git diff --check` 通过。未调用赛前真实赛事模型。
