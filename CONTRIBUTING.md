# 团队协作

仓库地址：<https://github.com/lucchinopolare-cmd/zhihu-hackathon-2026>。按私有仓库建立，由仓库所有者邀请队友。没有访问权限时，先接受 GitHub 的协作者邀请。

## 获取项目

在希望存放项目的父目录运行：

```powershell
git clone https://github.com/lucchinopolare-cmd/zhihu-hackathon-2026.git
cd zhihu-hackathon-2026
```

先读根目录 [项目档案](项目档案.md)，再读 [产品需求文档](docs/产品需求文档.md) 和 README。2026-09-11 已确认允许跳过复述直接使用 AI；历史骨架本地测试与离线 probe 通过，网页、应用模型和公网验收尚未完成。产品名、技术栈与分工待确认。

## 分工和提交

确定选题后，再按页面、内容接口、模型接入、演示材料分配互不重叠的任务。开始修改前在团队内确认文件归属。

从更新后的 `main` 新建自己的分支，例如 `feat/reading-page`、`fix/api-timeout`、`docs/demo-script`。推送后发起 Pull Request，说明改动目的、实际验证和仍待处理的问题。合入前由另一位队友检查；这是一项团队协作建议，仓库尚未启用强制分支保护。

正式恢复开发时，新增或修改功能应执行相关检查。失败和未运行的检查如实记录，不填“全部通过”。保留真实提交时间和 AI 辅助记录。

## 凭证与本机文件

模型 API key 在本机配置，仓库不共享个人令牌。`.env`、`.env.*`、`.local/`、缓存和日志已忽略；将来可以提交只含变量名与空值的 `.env.example`。现在尚未定义提供商环境变量，不要猜测接入参数。

`sources/` 保存的原始 API 样本和下载包没有上传。含个人课表的早期方案留在本机，团队讨论使用 `docs/项目方向草案.md`。

换 API 时同时区分两处配置：开发工具 Codex 使用的模型服务，以及参赛应用运行时使用的模型服务。它们可以不同，配置其中一个不会自动改变另一个。

## 技能

见 [技能与工具](docs/技能与工具.md)。全局安装在某位队友电脑上的 skill 不会随 `git clone` 自动安装，按固定来源安装或让兼容的 Agent 读取对应文档即可。
