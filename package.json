{
  "name": "zhixing-learning-workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "demo": "node src/demo-server.mjs",
    "dev": "node --env-file-if-exists=.env --watch src/server.mjs",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "probe": "node scripts/probe-knowledge.mjs"
  }
}
